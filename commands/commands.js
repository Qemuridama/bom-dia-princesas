const say = require("./each/say");
const repeat = require("./each/repeat");
const help = require("./each/help");
const ping = require("./each/ping");

module.exports = {
  say,
  repeat,
  help,
  ping
}